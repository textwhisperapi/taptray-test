# TapTray Test

Working copy cloned from the TextWhisper test project and repointed to the `taptray` database.

## Daily Git workflow

```bash
git status
git add -A
git commit -m "Describe your change"
git push
```

## First-time clone on another machine

```bash
git clone <taptray-repo-url>
cd taptray-test
```
